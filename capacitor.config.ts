import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snowball.taskmanager',
  appName: 'Snowball Task Manager',
  webDir: 'dist'
};

export default config;
